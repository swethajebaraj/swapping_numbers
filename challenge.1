#include<stdio.h>
main()
{
	int a=10;
	int b=20;
	
	printf("Before swapping a=%d and b=%d\n",a,b);
	a=a+b;  //if a=10,b=20,then a=a+b=30
	b=a-b;  //also b=a-b=30-20=10
	a=a-b;  //and a=a-b=30-10=20
	        //now a=20,b=10
	printf("After swapping a=%d and b=%d",a,b);      
}
